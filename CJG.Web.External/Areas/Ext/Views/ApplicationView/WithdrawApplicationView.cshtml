<div>
    <div class="dialog-header">
        <h3><b>{{ngDialogData.title}}</b></h3>
    </div>
    <div class="dialog-content">
        <div class="modal-content">
            <div ng-controller="ApplicationDetailsWithdrawView" ng-grant-application-id="@ViewBag.GrantApplicationId" ng-row-version="@ViewBag.RowVersion">
                <div ng-if="alert.message" class="row row--alert">
                    <div class="alert" ng-class="'alert--' + alert.type" role="alert" ng-bind-html="alert.message"></div>
                </div>
                <div class="content--withdraw-application">
                    <p>You may withdraw your application at any time if, for example, you have cancelled your training plans or have applied under the wrong stream.</p>
                    <div class="form__group">
                        <label class="form__label form__label--align-top" for="withdraw-reason">Please provide a reason</label>
                        <div class="form__control">
                            <textarea ng-model="model.WithdrawReason" id="withdraw-reason" class="input--full" maxlength="800"></textarea>
                        </div>
                    </div>

                    <div class="button-group">
                        <button name="btnCancelWithdraw" class="btn btn--secondary" ng-click="closeThisDialog()">No, continue with application</button>
                        <button class="btn btn--primary btn--forward confirm-link pull-right" ng-disabled="!model.WithdrawReason" ng-click="withdraw()">
                            Yes, withdraw application
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



